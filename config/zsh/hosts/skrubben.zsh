# Host-specific zsh config for: skrubben (Desktop PC)
# This file is sourced by .zshrc based on hostname

# Desktop-specific aliases (if any)
# alias gpu-fans='nvidia-settings -a "[gpu:0]/GPUFanControlState=1"'

# Desktop-specific environment variables (if any)
# export SOME_DESKTOP_VAR="value"
