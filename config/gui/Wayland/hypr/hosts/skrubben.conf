# Host-specific config for: skrubben (Desktop PC)
# This file is sourced by hyprland.conf based on hostname

# Monitor configuration
# Adjust these based on your actual monitor setup
# Use `hyprctl monitors` to see connected displays
monitor=DP-1,preferred,0x0,1
monitor=DP-2,preferred,1920x0,1
monitor=HDMI-A-1,preferred,auto,1

# Desktop-specific startup (if any)
# exec-once=...

# No lid switch handling needed for desktop
