#!/bin/bash
# Pi-only adoption; does not run desktop installation or restart services.
set -euo pipefail
ROOT="$(cd "$(dirname "$(readlink -f "$0")")/.." && pwd)"
exec python3 "$ROOT/scripts/pi_setup.py" "$@"
