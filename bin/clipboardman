#!/bin/bash
set -euo pipefail

# Clipboard manager using cliphist and wofi (Wayland only)
if [[ -z "$WAYLAND_DISPLAY" ]]; then
    echo "clipboardman requires Wayland (WAYLAND_DISPLAY not set)" >&2
    exit 1
fi

cliphist list | wofi --dmenu | cliphist decode | wl-copy
